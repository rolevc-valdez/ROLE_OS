# 07 — Roadmap

This is a record of what has shipped, in order, and the seams left open
for what could come next. For full detail on any entry, see `CHANGELOG.md`
at the repo root — this page is the condensed map; the changelog is the
source of truth.

## Shipped

| # | Name | What it added |
|---|---|---|
| M1 | Knowledge API | `GET /health`, `/projects`, `/search?q=`, `/knowledge/{id}` over the Builder-generated SQLite DB. No AI features. |
| M2 | First usable dashboard | Web UI served at `/`: global search, project list, recent cards, card detail, timeline. New additive `GET /ui/recent`, `GET /ui/timeline`. |
| M3 | Knowledge Engine 2.0 | Modular extractor pipeline (`builder/extractors/`): summary, decisions/deliverables, todos, prompts, entities (people/apps/vendors/urls/files), relationships (`related_conversations`). New `VENDORS.json` index. |
| Epic 1 | Project Intelligence | First-class Workspaces, Projects, Capabilities, Dependencies, and a modular Health Score engine. New `/pi/*` API and Projects UI tab. |
| Epic 2 | AI Advisor | Eight deterministic rules recommending what to do next, each explainable (`reason`/`evidence`/`suggested_action`/`impact`). New `/advisor/*` API and Advisor UI tab. `AdvisorNarrativeProvider` seam introduced. |
| Epic 3 | Knowledge Graph | 12 node types, 12 relationship types, computed on demand from the other three databases — no new database. New `/graph/*` API and Graph UI tab with SVG visualization. |
| Epic 4 | Command Center | Full UI redesign: persistent sidebar/header, hash router, Home/Project/Graph/Advisor/Assets/Settings pages, reusable design system, `createGraphView()` factory. UI-only — zero new backend surface. |
| Alpha | Alpha demo | `scripts/run_alpha.sh` / `.bat` one-command setup + seed script (`scripts/seed_alpha_demo.py`) populating seven realistic demo projects across five workspaces, so the whole system is explorable without bringing real data first. |
| B1 | ChatGPT Conversation Importer | Dashboard-owned import pipeline (`dashboard/app/imports/`) for bringing ChatGPT exports in directly — parse, normalize, deduplicate, persist, report — without regenerating the Builder-generated knowledge base. New `/import/*` API, `scripts/import_chatgpt.py` CLI, and a Knowledge page import panel. No AI extraction, project matching, or graph linking. |
| B1.5 | Conversation Explorer | Browse/search/filter/inspect/manage UI over imported conversations: new Explorer sidebar page (metrics strip, search/filter/sort, paginated table, detail view with message timeline + metadata panel), extending `/import/*` with search/filter/sort/pagination, detail, JSON export, delete, facets, and metrics endpoints. No AI, extraction, project matching, or graph inference. |
| Sprint 4 | Knowledge Extraction | Deterministic, rule-based extraction of Project/Person/Task/Decision/Idea/Document/Asset objects from imported conversations (`dashboard/app/extraction/`), each tracked with conversation id, source, confidence, and created/updated dates. New `/extraction/*` API; a Knowledge section (Extract/re-run + seven object-type lists) added to the Explorer's conversation detail view; Explorer dashboard metrics now report real per-type counts. No AI, no summarization, no graph, no advisor. |
| Sprint 5 | Knowledge Graph | A second, independent graph (`dashboard/app/conversation_graph/`) over imported conversations and extracted objects — 8 node types, one `contains` relationship, computed on demand from the imports/extraction databases, deliberately separate from Epic 3's frozen 12-type `/graph`. New `/conversation-graph/*` API; a new Knowledge Graph sidebar page (reusing the existing `createGraphView()` engine) with conversation/node-type filters and a node detail panel; two-way links with the Conversation Explorer; Explorer dashboard metrics gain real `graph_nodes`/`graph_edges` counts. No AI, no inferred relationships, no graph database. |
| Sprint 6 | Advisor Search | Keyword/partial-match search over imported conversations and extracted objects, added to the existing Advisor page as a new "Search Knowledge" section (two new files inside `app/advisor/`: `search.py`, `search_models.py`; Epic 2's recommendation engine untouched). New `/advisor/search/*` API (own router, same `/advisor` prefix); results link straight to the Conversation Explorer and the Sprint 5 Knowledge Graph via a shared `graph_node_id`. No AI, no NLP, no embeddings, no semantic search. |
| Sprint 7 | Dashboard *(superseded by Sprint C2, below)* | A new executive-summary sidebar page (separate from Home, which keeps covering the Project Intelligence/Advisor/Epic 3 Graph pipeline) over the Importer/Explorer/Extraction/Knowledge Graph/Advisor Search pipeline: ten summary cards (all read from the existing `GET /import/metrics`), Recent Activity, System Status, and Quick Actions to every related page. Two thin, additive endpoints (`GET /extraction/recent`, `GET /extraction/runs`); zero new storage, zero new calculation logic, zero AI. **Replaced by Sprint C2** once real workspace data existed and this page's metrics stayed at zero regardless. |
| Sprint 8 | Settings | A Settings page backed by a real API, replacing the earlier `/health`-only placeholder: `GET /settings` (general/system/about/maintenance in one response), `GET /settings/export`, `POST /settings/import` (validate + preview only, never applied), `POST /settings/maintenance/rebuild-graph`, `POST /settings/maintenance/clear-cache`. Two new `Settings` fields (`default_import_path`, `search_result_limit` — the latter now the Advisor Search API's configurable default `limit`). No new storage, no AI. |
| Sprint 1 (Discovery) | Discovery Engine | Read-only, CLI-only filesystem audit (`dashboard/app/discovery/`) implementing Phase 1 of `08_IMPORT_ENGINE_PROPOSAL.md`: scanner, detectors, git reader, classifier (kind/move-risk/maturity/commercial-readiness), an 8-signal Health Score, and a six-action Recommendation engine (`Leave where it is`/`Move into IA PROJECTS`/`Archive`/`Merge with another project`/`Rename`/`Requires manual review`), each with explainable reasons. `python -m app.discovery audit --root <path>` reports classification, git status, docs/license/tests/CI presence, asset counts, and move-safety findings (absolute paths, `.env`, launchers, Obsidian vaults, VS Code workspace files) as JSON/Markdown/console. No API route, no database, no project import yet — that's Sprints 2-4 of the same proposal. |
| Sprint 2 (Workspace Adoption) | Workspace Adoption | The first writable layer over the Discovery Engine (`dashboard/app/workspace/`): a new "Workspace" sidebar page listing every discovered folder (Name/Folder/Type/Git/Health/Confidence/Move Risk) with Adopt/Ignore/Review actions and a Rescan button; adopting creates a small overlay record (priority/business value/status/tags/notes/ignored) in its own new SQLite file, never duplicating discovery metadata — everything else is re-read from a cached scan. Projects page now also shows adopted projects (labeled "Discovered") alongside manually-created ones. New `/workspace/*` API; no changes to Advisor, Graph, Health, or Mission Control yet — that's Sprints 3-4 of `08_IMPORT_ENGINE_PROPOSAL.md`. |
| Sprint 3 (Project Boundary) | Project Boundary / Hierarchy | Fixes the Workspace page presenting every discovered folder as a flat peer row: a new `item_kind`/hierarchy model (`dashboard/app/discovery/boundary/`) distinguishing top-level project / nested repository / component / documentation / asset library / internal folder / excluded / non-project, computed generically (no name-specific logic) via a whole-scan corpus pass. Configurable exclusions (one source-of-truth `exclusions_config.json`: exact/case-insensitive/glob/relative-path patterns, plus user extras) recursively skip folders like `OTROS - no proyectos`. Workspace page now groups top-level projects with rolled-up repository/component/internal-folder counts and an Expand action, plus filter tabs (Top-level/Nested repositories/Ignored & excluded/Needs review) and a user override (`treat as top-level` / `attach to parent`) stored separately from the Discovery Engine's own computed boundary. `GET /workspace/discovered`'s no-`view` contract is unchanged. Verified against the real `1 - IA PROJECTS` folder: 17 flat rows collapse to 4 top-level projects. |
| Sprint 4 (Project Intelligence Wiring) | Project Intelligence Wiring | Wires Sprints 1-3's discovered/adopted project data into Projects, Home, Advisor, and Assets, which previously showed empty/zero-centric content with no manual Project Intelligence data. New: a deterministic Next Action extractor (`app/discovery/next_action.py`, 7-source priority order, no LLM), a read-only asset discovery index (`app/workspace/assets_index.py`), Workspace Advisor 2.0 (11 evidence-only rules, `app/workspace/advisor.py`, a sibling to Epic 2's advisor), a unified Recent Activity feed, and Home portfolio aggregation (Last Active/Most Recently Modified/Needs Attention/Recent Commits/Recent Assets/Latest AI Session/Suggested Project/Quick Resume). New additive `/workspace/home`, `/workspace/advisor`, `/workspace/assets`, `/workspace/activity` endpoints; `/workspace/discovered?view=top_level` is now enriched; `/workspace/summary` gained stale-data fields. New Discovered Project Detail view (`#/dproject/{id}`) parallel to the existing manual-project detail view, showing "Not yet defined" rather than inventing missing data. Scoped to *adopted* top-level projects only. Verified end-to-end in a live browser against the real `1 - IA PROJECTS` folder. |
| Sprint 5 (Project Unification) | Project Unification | Removes the conceptual split between manually-created Projects and discovered/adopted projects: a canonical Project Identity bridge (`projects.discovery_item_id` ↔ `adopted_projects.canonical_project_id`, `app/workspace/identity.py`), resolved lazily/idempotently, never duplicating discovery metadata into the Project row. New Resume Work orchestration (`app/workspace/resume.py`, `POST /workspace/discovered/{id}/resume-work`) finds-or-creates the latest AI Session, builds the Resume Prompt, and resolves the assistant URL — wired as the primary action on Discovered Project Detail, Home's Quick Resume, and every Workspace Advisor recommendation. Fixes Sprint 4's documented gap (AI Sessions/Timeline were querying with the wrong id and always returning empty); adds real Timeline to Discovered Project Detail. Backward compatible: existing manual projects unaffected, a name-match migration links them where possible, Projects page dedupes. Verified end-to-end in a live browser/API session against the real `1 - IA PROJECTS` folder. |
| Sprint C1 (Consolidation) | Project Context | A single reusable `ProjectContext` builder (`app/project_context/`) assembling identity/health/git/commits/next action/normalized advisor summary/asset-document-knowledge counts/timeline/resume state from Discovery/Workspace/Project Intelligence/Advisor exactly as they already work — no rewrite of any of the four. New additive `GET /project-context` (bulk) and `GET /project-context/{id}` API. Fixed two real bugs found while centralizing: `get_home_portfolio`'s `latest_ai_session` was silently always `None`, and `get_enriched_item` queried AI sessions twice per call. Added a server-side `project_id` filter to `/workspace/activity`/`/workspace/assets`, replacing a client-side whole-feed filter on the Discovered Project Detail page. Wired Cockpit's Next Action card to the shared service (falls back to its prior computation on failure). Verified end-to-end in a live browser against the real `1 - IA PROJECTS` folder. Known scope limit: an independent audit found this module had exactly one production caller — recommended a rewiring sprint. |
| Sprint C1B (Rewiring) | Project Context — Load-Bearing | Made `ProjectContext` load-bearing: `GET /workspace/home`, `GET /workspace/discovered?view=top_level`, `GET /workspace/discovered/{id}`, `GET /pi/projects`, `GET /pi/projects/{id}`, `GET /workspace/advisor`, and `GET /advisor/recommendations` now all embed a real `project_context` per project/recommendation. Removed the duplicate logic the C1 audit found: the inline next-action mini-extractor (now routes through `discovery.next_action.extract_next_action` for every project, discovered or manual), the disconnected `resume_state` stub (now `workspace.resume.preview_resume_state()`, a real read-only mirror of Resume Work), the cheap asset-count that could disagree with the real Assets index, and the health-tier threshold contradiction (one canonical `app/project_context/health.py`, 80/50, synced to the frontend fallback). Cockpit reads its embedded `project_context` instead of a separate fetch. 13 new tests (`test_project_context_rewiring.py`); full suite 909 passed. Verified live against the real workspace. See `15_PROJECT_CONTEXT_SPRINT_C1B_REPORT.md`. |
| Sprint C2 | Dashboard 2.0 | Replaces the legacy zero-centric Dashboard (Sprint 7, which showed Explorer's own `/import/metrics` counts) with an executive dashboard powered by `ProjectContext` and the existing Home/Advisor/Activity/Assets/Session/Knowledge/Workspace services. One new additive endpoint, `GET /dashboard/summary` (`app/dashboard/service.py`), a composition layer — not a new aggregation engine — over `ProjectContext`, `workspace.service.get_home_portfolio`, `workspace.advisor.generate_recommendations`, `workspace.service.list_activity_feed`/`list_project_assets`, and `app.db`'s Knowledge counts. One genuinely missing rule added to the existing Workspace Advisor rule set (`rule_snapshot_blocker`, surfacing a blocker from a project's latest AI session snapshot). Executive summary cards, Portfolio Status groups (Healthy/Warning/Critical/Active/Inactive/Launch-ready), Continue Work (reuses the existing suggestion ranking), Needs Attention, Recent Activity/Assets/Knowledge, and honest empty states, all presentation-only (no client-side recalculation of health/next-action/recommendation-priority/resume-availability). The legacy rendering path was fully removed, not left underneath. 21 new/rewritten tests; full suite 920 passed. Verified live against the real workspace: real projects and metrics replaced the all-zero display, Resume Work worked end-to-end. See `16_DASHBOARD_2_0_SPRINT_C2_REPORT.md`. |
| Sprint C2.1 | Project Identity Reconciliation | Non-destructive project-merge tooling fixing a real duplicate ("ROLE Commerce Factory" appearing as two separate projects) without a data-loss-risking migration: a merge preview/apply flow that reconciles the manual-Project and discovered/adopted identities into one, preserving history rather than deleting either row outright. |
| Sprint C3 (+ C3.1 hardening) | Explorer 2.0 | Removes Explorer's entire legacy Conversation Explorer browsing dependency (Imported Conversations list, conversation database counters, import statistics) and replaces it with universal `ProjectContext` search: `GET /explorer/search?q=` across 13 result types (project, discovered folder, AI session, decision, document, asset, person, application, task, idea, deliverable, note, activity), each with real navigation actions, auto-running on page load. `GET /explorer/project-hub/{id}` composes a per-project hub view. C3.1 hardening: audited every endpoint Explorer actually called, removed the legacy conversation-browsing UI entirely (kept only the shared conversation detail overlay), and consolidated a genuine duplicated-aggregation bug (`dashboard.service`/`explorer.service` each keeping a private copy of "every tracked project") into one shared `project_context.builder.all_project_contexts()`. |
| Sprint C4 | Assets OS | Replaces the Assets page's flat technical file listing (Knowledge Graph `Asset` nodes) with a real visual Asset Library over files discovered inside adopted projects: a canonical `AssetRecord` model (`app/assets/model.py`) reused by the Assets gallery, Explorer's Asset results, and Project Hub — no second asset mapper. Deterministic, no-LLM classification into 16 categories; reusable-by-default rules with user overrides stored only in a new `role_os_assets.db` (never touching source files); duplicate detection via partial-content hash, grouped but never auto-consolidated; a safe preview/thumbnail service (`app/assets/preview.py`, Pillow-based) that resolves every request exclusively through a validated `asset_id` already in the live index (`resolve_safe_path`), never a client-submitted path, with decompression-bomb and path-traversal protection; one canonical `/assets` API, with `/workspace/assets` (Sprint 4) now delegating to it. 43 new tests; full suite: 1011 passed, 0 failed. Verified live against the real `1 - IA PROJECTS` folder, including bugs found only by live verification (a `\b`-boundary classification regex that `_`/`-` defeated, an uncaught `PIL.DecompressionBombError` on a real 208-megapixel image, classification matching a file's entire absolute path instead of the project-relative one, and the generated thumbnail cache directory leaking back into its own index from two different process cwds) — all fixed and covered by regression tests. See `17_ASSETS_OS_SPRINT_C4_REPORT.md`. |
| Sprint C4.1 | Assets Canonicalization Audit | An audit sprint, not a feature sprint: verifies Assets/Explorer/Project Detail/Project Hub/Home/Dashboard/Advisor/ProjectContext all consume the same canonical `AssetRecord`/`app.assets` service, with zero independent classification/hashing/MIME-detection/duplicate-grouping logic anywhere else in the repo (confirmed via a full symbol-level caller audit). Found and fixed one real cross-screen bug: `index_project_assets` (called directly by Dashboard, Home, `ProjectContext.assets_count`, and Project Hub) never resolved `duplicate_group_id` to `None` for a genuinely unique file — only the `/assets` API's `list_all_assets` applied that correction, so every other screen showed a false "this has a duplicate" signal for the same real asset the Assets API correctly showed as unique. Fixed by resolving duplicates inside `index_project_assets` itself. Confirmed the Epic 3/Sprint 5 Knowledge Graph "Asset" node types are a legitimate, separate, pre-existing concept (knowledge-extraction/manual-entry, not filesystem) with no id/endpoint overlap with `app.assets` — documented, not merged. Reviewed classification.py for growth risk (163 lines, left as-is) and the duplicate-detection service boundary (stays inside `app.assets`, extension points documented, nothing implemented). Measured (not fixed, out of audit scope) one real performance inefficiency: `GET /dashboard/summary` walks every project's filesystem twice per request. 9 new architectural guard tests (source-tree inspection via `ast`, not behavior) that fail automatically if a second classifier, duplicate grouper, override-writer, or client-side classification logic is ever reintroduced. Live-verified full count parity (Assets API = ProjectContext = Project Hub = 9 for the real `ROLE_KNOWLEDGE_OS` project) and field-level identity (`shot_4_logo.png` identical across every surface). No new features, no UI changes, no new database. See `18_ASSETS_CANONICALIZATION_SPRINT_C41_REPORT.md`. |
| Sprint C5 | Mission Control | The daily operating surface: one new endpoint, `GET /mission-control` (`app/mission_control/service.py`), composing `ProjectContext`, Home's ranking (`get_home_portfolio`/`suggested_project_to_continue`), `workspace.advisor.generate_recommendations`, `workspace.service.list_activity_feed`, and the existing Daily Session domain (`app.session.db`) into one already-shaped payload: Primary Focus (one recommended project + why), Today's Focus (top 3 actionable items), Since Last Time (real changes since the last Daily Session, or a labeled 24h fallback), Needs Attention (severity-sorted), a Value Signal ("Closest to Launch," honest when no project qualifies), a compact Portfolio strip, deduplicated Recent Activity, Daily Session state, Snapshot Continuity, and Quick Actions. No new ranking/recommendation engine — every field reuses an existing service exactly once. Home now routes to Mission Control (`renderMissionControlPage`); Dashboard is unchanged and remains the deeper executive analytics view. Fixed the Sprint C4.1 performance finding: `app.assets.service.request_scope()`, a `contextvars`-backed cache keyed by resolved root path, collapses what used to be 3-4 filesystem walks per adopted project per request (in both `/dashboard/summary` and `/mission-control`) down to one. 14 new tests (`test_mission_control_api.py`), including a regression test proving the walk-count fix; full suite 1035 passed. Verified live against the real `1 - IA PROJECTS` workspace (6 tracked projects, ~70-300ms response): a real primary recommendation with reasons, real Today's Focus/Needs Attention/Value Signal/Recent Activity/Portfolio, and Resume Work working end-to-end from the Mission Control card. Known limitation: Since Last Time can only surface event types the existing Recent Activity feed already tracks (no status-change/blocker/roadmap-edit events yet) — candidate for C6. See `docs/product/DECISIONS.md` for the full reasoning. |
| Sprint C6 | Operational Intelligence Engine | One canonical service, `app.operational_intelligence.get_operational_intelligence()` (`app/operational_intelligence/`), composing the two pre-existing rule engines (`workspace.advisor` -- discovery/git/health evidence; `app.advisor.engine` -- PI dependencies/capabilities/TODOs/deliverables, reused via its own already-persisted orchestrator so Advisor's dismiss/complete/TTL lifecycle stays intact) plus three genuinely new deterministic rules (`rules.py`: Knowledge freshness, Discovery scan freshness, workspace-status-with-pending-work) into one normalized shape carrying `recommendation, priority, confidence, evidence, project, expected_benefit, suggested_action` (the brief's exact seven fields) plus `reason/action_link/source/rule_id`. No LLM/embeddings/vector DB/external AI API -- every recommendation is a deterministic rule over already-computed evidence. Conflict resolution: same-title-per-project collisions collapse to the higher-priority instance; different recommendations never collapse. Priority: no new scoring formula, only sorts by each rule pack's own already-0-100 value. `expected_benefit`: a static, documented keyword-lookup table (`models.py`), never generated. Mission Control's Today's Focus/Needs Attention/Value Signal/Daily-Session-suggestion now consume this one engine instead of calling the discovery pack directly; Advisor gained an additive `GET /advisor/operational-intelligence` endpoint (existing `/advisor/recommendations` unchanged); Explorer's Recommendation results now expose an `evidence` field. Mission Control passes its already-computed `all_project_contexts()` result straight through so the engine never recomputes it a second time per request. 10 new tests (`test_operational_intelligence.py`) covering the seven-field shape, sort order, both reused rule packs, the three new rules, the dedup mechanism, and every consumer; full suite 1045 passed. Verified live against the real `1 - IA PROJECTS` workspace: `GET /advisor/operational-intelligence` returns 14 real, correctly-sorted recommendations in ~0.4s; Mission Control's cards show real "Benefit: ..." text and a Daily Session suggestion sourced from the engine's top recommendation for ROLE_OS. Known limitations: Resume Work's own prompt mechanics are unchanged (its integration point is contextual, via the Mission Control card that triggers it, not its own scoring); Dashboard was not deeply rewired this sprint (optional per the brief) and still reads the discovery pack via `ProjectContext.advisor_summary`. See `docs/product/DECISIONS.md` for the full reasoning. |
| Sprint C7.1 | Resume Work Refactor | Fixes a real product flaw real-world validation surfaced: Resume Work resumed an *AI Session*, not a Project -- a thin session/snapshot meant a thin, generic prompt. New `app/project_memory/` layer: Project Memory (`service.py`) is the source of truth for the Resume Prompt, composed from `ProjectContext` (next action, git, latest snapshot) plus the Sprint C6 Operational Intelligence Engine's top recommendation; the AI Session is only ever the transport. The Resume Prompt (`prompt.py`) now always begins with `Project:`, `Current Objective:`, `Where We Left Off:`, `Pending Work:`, `Next Action:`, `Operational Recommendation:`, `Conversation:`, in that order -- session data appears only in `Conversation:`. Conversation selection (`session_selection.py`): latest active session > pinned (`favorite`) > preferred (`current`) > newest, each with a plain-English reason. Session naming (`naming.py`): every created/retitled session is named `<Project Name> — <Objective>`, never "Resume Work"/"Untitled"/"Session 1" -- an existing session literally titled "Resume Work" (the old bug) self-heals to the new name the next time it's resumed. Cockpit: Project Memory replaces the old Today's Objective/Next Action/Last Snapshot insight cards as the primary card; AI Sessions is now a secondary section. Fixed a real recursion (`preview_resume_state` building Project Memory, which builds `ProjectContext` again) with two new default-`True` cost knobs on `build_project_context` (`include_resume_state`, `include_epic2_recs`) that only `app.project_memory` sets to `False` -- and a real performance regression caught by the sprint's own regression run (two existing test files went from a few seconds to over two minutes because every `ProjectContext` build was newly nesting a second Epic 2 Advisor refresh), fixed by making every caller except the one primary Resume Work click skip the Operational Intelligence lookup. Removed (not deprecated) the old session-only `app.services.resume.build_resume_prompt` and its test file. 27 new tests (`test_project_memory.py`) plus updates to `test_workspace_resume.py`/`test_ai_sessions_api.py`/`test_cockpit_redesign_ui.py`; full suite 1067 passed. Live-verified against the real `ROLE Commerce Factory` project: Resume Work produces a complete, project-grounded prompt with a real Operational Recommendation and an explained conversation choice. See `docs/product/DECISIONS.md` for the full reasoning. |
| Sprint C8 | Project Ecosystem Engine | ROLE OS now understands how adopted projects relate to each other, from deterministic evidence only (no LLM/embeddings/vector DB). New package `app/project_ecosystem/` (`models.py`, `detectors.py`, `relationships.py`, `graph.py`, `service.py`, `db.py`) and one new endpoint, `GET /project-ecosystem/{id}`. A canonical relationship model (`relationship_id, source_project, target_project, relationship_type, confidence, evidence, detector, discovered_at, last_verified, manual_override, status`; `relationship_type` one of 13 supported types) is produced by pure detectors, each reusing an existing canonical domain rather than re-deriving it: PI's explicit dependencies/capabilities tables (`depends_on`/`uses`/`produces`, confidence 1.0, plus derived `blocks`/`blocked_by` from a blocked/at-risk dependency target); the canonical Assets index's already-resolved `duplicate_group_id` (`shares_assets`); Knowledge cards soft-matched to projects the same way `ProjectContext`/Explorer already do (`shares_knowledge`); bounded (20KB) reads of README/ROADMAP/CHANGELOG/TODO/NEXT_ACTION and git remote URLs searched for another project's name (`shares_documentation`/`related`); Session Snapshot/AI Session name-mentions (`shares_prompts`/`shares_sessions`); and sibling-folder structure (`related`). Conflict resolution merges same-pair-same-type relationships across detectors; a new `role_os_ecosystem.db` overlay stores only manual dismiss/confirm overrides, never the relationships themselves (always recomputed fresh). Impact Summary (`affected_projects, shared_assets, shared_documents, shared_prompts, shared_knowledge, shared_sessions, risk, confidence`) is bounded to direct relationships only -- no multi-hop graph dump. Wired into Explorer's Project Hub (a new Ecosystem section: clean cards, not a graph visualization) and relationship search (`_search_ecosystem`, "Used by ..." / relationship-keyword search); Mission Control's Operational Intelligence Engine gained `rule_unblocks_dependents` ("Complete X to unblock Y, Z"), reading only the cheap dependency detector to preserve OI's own no-repeated-scans contract; Project Memory gained a small, bounded `related_projects` section. Found and fixed a real pre-existing bug while building the shared-assets detector: `app.assets.service.group_duplicates` only ever cleared `duplicate_group_id`, never (re)set it, so `list_all_assets` could never actually resolve a cross-project duplicate despite its own docstring's promise -- fixed at the root, with a new regression test. Every whole-workspace pass (compute_relationships, Explorer search/project_hub, Resume Work, Cockpit's memory card) runs inside `app.assets.service.request_scope()` to avoid repeating the shared-assets filesystem walk. 23 new tests (`test_project_ecosystem.py`) plus a new regression test in `test_assets_os.py`; full suite 1091 passed. Live-verified against the real `1 - IA PROJECTS` workspace: real dependency/blocking/shared-documentation relationships detected among ROLE OS, ROLE Commerce Factory, ROLE MASTER, ROLE_KNOWLEDGE_OS, and role-ecosystem. Known limitations: no import/package-reference (source-code parsing) detection -- too language-specific and expensive at this sprint's scope; shared-prompts/shared-sessions detection is a simple name-mention scan, not semantic. See `docs/product/DECISIONS.md` for the full reasoning and C9 candidates. |
| Sprint C9 | Impact Analysis Engine | Answers "if this project changes, what else is affected?" by reading the Project Ecosystem Engine's (C8) already-computed relationship graph, `ProjectContext`, Assets, Knowledge, Operational Intelligence (C6), and Project Memory (C7.1) -- no new relationship-detection pass, no new graph. New package `app/impact_analysis/` (`models.py`, `scoring.py`, `service.py`, `api.py` inside the package itself, per this sprint's explicit deviation from the `app/routers/` convention) and one new endpoint, `GET /impact-analysis/{id}`. A canonical `ImpactReport` (`project, generated_at, overall_risk, confidence, affected_projects, direct_dependencies, transitive_dependencies, shared_assets, shared_prompts, shared_documentation, shared_knowledge, shared_sessions, operational_effects, release_effects, recommended_actions, evidence, limitations`) is built by a bounded (3-hop), cycle-safe BFS over the Ecosystem Engine's `depends_on` edges (reversed) plus its `shares_*`/`blocks` edges -- no traversal logic exists anywhere else. Risk (`scoring.py`) is five explainable levels (`none, low, medium, high, critical`) from a fixed, documented count-threshold table, each level returning the exact reason string that produced it. Wired into Explorer's Project Hub (a new "Impact Analysis" card section: Overall Risk, Affected Projects, Top Reasons, Recommended Actions -- no diagram); Mission Control's Operational Intelligence Engine gained `rule_high_impact_change` ("Changing X today will affect N project(s) -- schedule accordingly"), reading only the cheap dependency-only relationships already in `bundle["ecosystem_dependencies"]` and doing its own bounded traversal (never calling the heavier Impact Analysis Engine, preserving OI's own no-repeated-scans contract); Project Memory gained a compact "Potential Impact" line; Explorer search gained a new `"Impact"` result type. Found and fixed two real bugs: (1) `project_ecosystem/models.py`'s `BLOCKING_STATUSES` incorrectly included `"critical"`, and `detectors.py` also matched a target's computed *health tier* of `"critical"` -- conflating "explicitly marked blocked" with "defaults to health_score=0 because it's new," which falsely flagged nearly every brand-new project as blocking its dependents; fixed at the root in `project_ecosystem`, re-verified against the brief's own worked example (ROLE OS -> ROLE Commerce Factory -> RoleValdez.com), `test_project_ecosystem.py`'s 23 tests unaffected. (2) `build_project_memory()` was calling `get_operational_intelligence()` (a whole-workspace Epic 2 Advisor refresh) twice per invocation once Impact Analysis needed its own copy for `operational_effects`; fixed by computing it once and threading the same result through both call sites -- the same compute-once-at-the-outermost-caller pattern now applied a third time (C7.1, C8, C9). 27 new tests (`test_impact_analysis.py`); full suite 1118 passed. Verified live against the real `1 - IA PROJECTS` workspace: ROLE_OS, ROLE Commerce Factory, ROLE_KNOWLEDGE_OS, ROLE MASTER, and role-ecosystem each returned an honest `medium`-risk report backed by real shared-documentation evidence (no PI dependency edges declared yet in this workspace, so no direct/transitive dependents were fabricated). Known limitations: transitive traversal follows only explicit `depends_on` relationships (C8) -- an undeclared dependency is not traversed; shared-evidence detection inherits the Ecosystem Engine's own limitations; operational/release effects are read from each affected project's existing signals, never independently assessed. See `docs/product/DECISIONS.md` for the full reasoning. |
| Sprint C10 | Executive Decision Engine | Moves ROLE OS from an information dashboard to a deterministic decision system: one call answers "what should I work on next?" using evidence from every existing domain -- no LLM/embeddings/AI API, every recommendation explainable. New package `app/executive_decision/` (`models.py`, `scoring.py`, `planner.py`, `service.py`, `api.py` inside the package itself, matching C9's convention) and one new endpoint, `GET /executive-decision`. A canonical `ExecutiveDecision` (`generated_at, recommended_project, decision_score, confidence, reason, expected_benefit, estimated_effort, estimated_duration, blocking_projects, projects_unblocked, commercial_value, technical_value, risk, dependencies, today_plan, expected_result, evidence, limitations`) is produced by scoring every adopted project once against a fixed, additive, fully-documented point table (`scoring.py`, nine contributors: Operational Intelligence priority, commercial value/launch-readiness, projects unblocked, already-blocking-dependents, Impact Analysis risk, Project Memory pending work, recent activity/staleness, project health, paused/blocked status penalty) -- never a hidden weighting, every non-zero contribution named in `evidence`. Conflict resolution (`service._sort_key`) sorts by `(decision_score desc, health_score desc, canonical_project_id asc)`, a total order that structurally cannot tie. Today's Plan (`planner.py`) is a single deterministic step for the winner ("09:00" a fixed label, no scheduling engine, no calendar integration); estimated effort/duration come from a static keyword lookup, the same convention `operational_intelligence.models.expected_benefit_for` established. No new detector or graph: reuses `all_project_contexts`/Operational Intelligence/the Ecosystem graph/Impact Analysis exactly once per request, and imports Project Memory's own `_pending_work`/`_next_action_output` directly rather than re-deriving them. Mission Control gained `executive_decision`/`ranked_projects` fields (computed inside the same `request_scope()` that already collapses the shared-assets filesystem walk to once per request); the frontend now leads with a "TODAY" card and a "Portfolio Ranking" section above the pre-existing (now supporting) operational cards. Explorer search gained a new `"Executive Decision"` result type. Found and fixed two real bugs: (1) the first Mission Control wiring placed the new call just after the existing `request_scope()` block closed, causing a filesystem walk per adopted project instead of one -- caught immediately by the pre-existing `test_no_double_asset_walk_per_project_per_request` regression test. (2) Live browser verification found the frontend's `RESULT_TYPE_ORDER` array had never been updated for C8/C9's `"Ecosystem Relationship"`/`"Impact"` result types either -- both had been silently un-renderable in the UI since those sprints shipped; fixed by adding all three missing types together. 31 new tests (`test_executive_decision.py`); full suite 1175 passed. Verified live against the real `1 - IA PROJECTS` workspace: ROLE Commerce Factory, ROLE_OS, ROLE_KNOWLEDGE_OS, role-ecosystem, and ROLE MASTER all ranked, ROLE Commerce Factory winning deterministically (71.6 pts) on real evidence, matching the brief's own worked example. Known limitations: the brief's 500ms performance target is not met on this real ~18-project workspace (Executive Decision's own logic costs ~2ms; the remainder is inherited, already-documented `all_project_contexts`/`compute_relationships` cost from C6/C8); Today's Plan is always exactly one step. See `docs/product/DECISIONS.md` for the full reasoning. |
| Sprint 9 | Release | The v1.0 release package: `QUICK_START.md`, `INSTALLATION.md`, `ARCHITECTURE.md`, `RELEASE_NOTES_v1.0.md`, `LICENSE.md`, `FINAL_RELEASE_CHECKLIST.md`, `CONTRIBUTING.md`, and a reorganized root `README.md` — plus catching up `dashboard/README.md` documentation for Sprint 8. Documentation only; version bumped to `1.0.0`. |

Each entry after M1 was additive: every prior API endpoint and UI surface
was left unchanged, confirmed by a regression test in that entry's own
test suite (see `CHANGELOG.md` for the passing counts recorded at each
stage, e.g. "226/226 passing repo-wide" as of Epic 4).

## Open seams (not yet built, but explicitly designed for)

These are documented extension points in the current codebase, not
commitments or a scheduled backlog — treat them as "if this is ever
built, here's where it plugs in," not "this is planned":

- **`AdvisorNarrativeProvider`** (`dashboard/app/advisor/narrative.py`) —
  a `Protocol` for a future LLM-backed narrative provider that would
  improve *wording* of recommendation summaries/reasons without touching
  rule logic, scoring, or persistence. `DeterministicNarrativeProvider` is
  the only implementation today and the rules/scoring remain the source of
  truth for *what* to recommend regardless of which provider is active.
- **Graph Query Engine as a headless library** (`dashboard/app/graph/queries.py`)
  — every query function (`neighbors`, `shortest_path`, `impact_analysis`,
  `search_nodes`, and the named convenience wrappers) is a pure function
  over an already-built `Graph`, with no dependency on the API or
  dashboard, so a future AI provider or script could reason over the graph
  without going through HTTP.
- **`commits` Health Score signal** (`dashboard/app/projects/health/commits.py`)
  — implemented as a pure function but always returns unavailable, since
  no git integration is wired up yet. Adding one is "give this file a real
  data source," not a new signal to design.
- **Screenshots** — `README.md`, `dashboard/README.md`, and `DEMO.md` all
  note that real screenshots aren't bundled yet; the placeholder locations
  (`docs/screenshots/home.png`, `projects.png`, `advisor.png`, `graph.png`,
  `project_detail.png`) are already referenced and waiting to be filled in.
- **Keyboard shortcuts** — `DEMO.md` explicitly notes the Alpha has no
  custom shortcut layer yet; every interaction is click-driven.

## Guiding constraint for anything added next

Per [[01_VISION]] and [[02_PRINCIPLES]]: no external AI/LLM API call, no
new data store where a read/compute layer would do, additive namespacing
that never breaks a prior endpoint, and (for UI work) no new backend
surface unless the data genuinely doesn't exist yet. Any roadmap item that
would violate one of these should be treated as a deliberate, called-out
exception — not a silent departure from how every prior Epic was built.

## Where to go next

- [[01_VISION]] — the product vision this roadmap serves.
- `CHANGELOG.md` (repo root) — the full, detailed history behind this table.
- [[../product/CHANGELOG_PRODUCT]] — the product-facing view of the same history.
- [[../product/DECISIONS]] — why key choices were made along the way.
