"""Project Intelligence domain (Epic 1).

First-class Projects, Workspaces, Capabilities, and Dependencies, plus a
modular Health Score engine (`app.projects.health`). Persisted in a
dashboard-owned SQLite database, separate from the builder-generated
knowledge database (see `app.config.Settings.projects_db_path`).
"""
