# Role OS 2.0 — Current State

*This file describes NOW. It is overwritten at every completed major task, not appended to. For how something happened, read `docs/PHASE_1_TASK_*.md`, `docs/PHASE_1_COMPLETION.md`, `docs/product/DECISIONS.md`, or `git log` — never this file.*

## Status

- **Phase:** Role OS 2.0, **Phase 2 — Core Reliability & Discovery Accuracy — COMPLETE** (see `docs/ROLE_OS_2_PHASE_2_PLAN.md` for the approved plan, `docs/PHASE_2_COMPLETION.md` for the closing validation record)
- **Current task:** None open. Phase 2 is formally closed. Phase 3 IN PROGRESS — P3.1, P3.2, P3.3 and P3.4 COMPLETE; P3.5 NOT STARTED; see `NEXT_ACTIONS.md`.
- **Last completed task:** Phase 2 Final Validation / Completion — full closing validation performed after a reported power-loss interruption (confirmed via git/docs that no closing-validation work had been done before the interruption, so this ran from scratch, not as a continuation). See `docs/PHASE_2_COMPLETION.md` for the full 14-section record.
- **Last completed commit:** `20b80df` (`docs: complete role os 2 phase 2`) — still verify against git, per the Recovery procedure below
- **Overall state:** Stable. All 5 planned Phase 2 tasks (2.1–2.5) are complete and were individually re-verified live during closing validation: Test Isolation holds (SHA256/size/mtime of all 8 canonical DBs identical before/after the full 1,372-test suite and a live server smoke test); Multi-Root Discovery capability re-confirmed (22 tests pass) while remaining deliberately unset in this environment; Mission Control re-confirmed live (`total_projects_tracked: 5`, real Executive Decision, `is_stale: true` correctly discounting confidence 0.72); documentation sync holds (`app_version` still `1.2.0`, README/ARCHITECTURE still describe 2.0); Runtime Data Hygiene holds (legacy `dashboard/var/role_os_dashboard/` still deleted, ROLE MASTER still `status: "active"`, `pi_ai_workspace`/`ai_workspace` still 0 rows and untouched); all 32 routers/136 API paths present; git repo healthy (`git fsck` clean, in sync with `origin/main`). Phase 1's core (Mission Control, Resume Work, canonical data, navigation, freshness/fallback honesty) remains fully intact. Full detail on every individual Phase 2 task remains in its own `docs/PHASE_2_*.md` file, referenced from the resolution matrix in `docs/PHASE_2_COMPLETION.md`.
- **Note on concurrent work:** a separate Claude Code session (different session ID, same user) committed `PROJECT_REGISTRY.md`, `audits/PHASE_2_BACKUP_REPORT.md`, an ecosystem decision log (`229c1a7`), and a yt-dlp personal utility guide (`e46c93d`) directly to this repo during/after Phase 2 — broader, Role-Ecosystem-wide or personal-tooling work, distinct from and outside this Phase 2 plan's scope. None of these were reverted or modified by Phase 2 closing validation; `229c1a7`/`e46c93d` are explicitly documented in `docs/PHASE_2_COMPLETION.md` §0 as out-of-scope, untouched commits.

## PHASE 3 STATUS

*Verify against `git log` first; trust the repository over this section if they disagree.*

- **Role OS 2.0 — Phase 2: COMPLETE** (`docs/PHASE_2_COMPLETION.md`, checkpoint `20b80df`). Do NOT repeat Phase 1 or Phase 2.
- **Phase 3: IN PROGRESS.** Scope: `docs/ROLE_OS_2_PHASE_3_SCOPE.md`. **P3.1 COMPLETE** (analysis), **P3.2 COMPLETE** (`domain`/`client_name`/`kind` + `completed` semantics), **P3.3 COMPLETE** (`docs/PHASE_3_TASK_3_DAILY_COMMAND_CENTER_UI.md`), **P3.4 COMPLETE** (`docs/PHASE_3_TASK_4_EXPLICIT_PROJECT_REGISTRATION.md`).
- **P3.3 delivered:** Mission Control (`/`) is now the Daily Command Center — domain chips (KONTOOR / UNGER / ROLE PERSONAL / CLIENTES, plus a secondary Unclassified chip only when needed), *What should I do now?* with a human **Why** and **Continue Working**, Needs Attention, *Pending Work / Next Actions*, Active Projects | Completed, Tools | Role Dashboard (**Open Role Dashboard** → `#/dashboard`, Dashboard v2), Where I Left Off; all prior sections kept in a collapsed "Full analysis" panel. Backend: additive `work` block in `GET /mission-control`. `kind = tool` and `completed` never compete in ranking. No URGENT / QUICK WIN / WAITING is shown (no real data).
- **P3.4 delivered:** Explicit Project Registration — Workspace page **Register Project** (text path → Validate → review card → Register; Adopt/Unregister as separate explicit actions). New table `registered_projects` in `role_os_workspace.db`; registered folders feed the same `_cached_projects()` as Discovery, get the same `compute_item_id` identity and the same read-only analysis, and are refreshed on rescan. Registration ≠ adoption: no overlay row, no classification, never in Mission Control until adopted. Drive roots / user profile refused; case/slash-insensitive dedupe. API: `POST /workspace/registrations/inspect`, `POST|GET /workspace/registrations`, `DELETE /workspace/registrations/{id}` (blocked while adopted or holding data). Discovery roots unchanged.
- **bolsa-de-trabajo:** REGISTERED (one `registered_projects` row, id `d560e18f1f2b3339`), **NOT adopted, NOT classified**, absent from Mission Control. Role decides whether to adopt it (Workspace → Registered project folders → Review → Adopt).
- **Canonical runtime data:** the five adopted projects (ROLE_OS, ROLE Commerce Factory, ROLE MASTER, role-ecosystem, ROLE_KNOWLEDGE_OS) are now persisted as `ROLE PERSONAL` / `project`, statuses unchanged (all `active`). Zero tools and zero completed projects exist; those sections show honest empty states. Nothing else was adopted.
- **Next task:** P3.5 — External Project Manifest / `ROLE_PROJECT.md`. NOT STARTED — see `NEXT_ACTIONS.md`.
- **DEFERRED — not approved work:** `var/role_os_alpha/` disposition; `pi_ai_workspace` deprecation/removal; Advisor/OI/Executive Decision overlap; registration/adoption of FERREVOLT / Kontoor / Unger (the P3.4 mechanism now exists; use it only on Role's instruction); adoption of bolsa-de-trabajo (Role's decision); Discovery root changes; `ROLE_PROJECT.md` (P3.5); Windows startup (P3.6); TASK entity, deadlines, URGENT/QUICK WIN/WAITING (only if real data supports them); registering tools such as yt-dlp/Cobalt; external-projects-DB test-row cleanup.

**If Role returns in a new AI session:** read `CURRENT_STATE.md`, `NEXT_ACTIONS.md`, `docs/ROLE_OS_2_PHASE_3_SCOPE.md`, then the P3.1–P3.4 docs. Do not begin P3.5 without Role's approval.

## What Is Working

- **Phase 2 is formally closed** (`docs/PHASE_2_COMPLETION.md`): all 14 closing-validation areas passed (git health, canonical paths, data integrity, full test suite, test-isolation checksums, Multi-Root Discovery, Mission Control smoke test, navigation/router regression, documentation, runtime data hygiene, crash recovery, AI handoff, Phase 2 resolution matrix). Every bullet below was independently re-verified live as part of that closing validation, not just carried over from earlier Phase 2 task docs.
- Canonical runtime paths (`var/role_os/`, `var/role_os_dashboard/`) are anchored to the repository root regardless of launch directory — re-verified via 17 passing CWD-independence tests and a live start from `dashboard/` (the historically CWD-sensitive launch directory).
- Real, verified project state — 5 adopted projects, 7 Daily Session registry rows — confirmed present with `PRAGMA integrity_check: ok` on both canonical databases.
- Mission Control (`GET /`, `GET /mission-control`) answers all three core questions with real data — confirmed live during Final Validation: Where I Left Off (ROLE OS), What Matters Now (ROLE Commerce Factory), What's Next (3 real Today's Focus items).
- Resume Work uses the existing, unmodified backend — no parallel implementation exists anywhere.
- Staleness and fallback honesty is genuinely active right now, not just tested: real time passing pushed `data_freshness.is_stale` to `true` during Final Validation itself, and the system correctly discounted Executive Decision's confidence (0.85 → 0.72) and displayed the stale/fallback UI elements.
- The legacy `project-dashboard.html` remains archived (byte-identical, re-verified); Role Master assets remain tracked and unmodified.
- Navigation remains grouped into 5 clusters; all 32 routers and every deep link remain reachable — re-verified live.
- `var/discovery_reports/` remains resolved — archived, no ambiguous active persistence location.
- The full test suite (`tests`, `dashboard/tests`, `builder/tests`) passes completely: **1,453 passed, 0 failed** at P3.4 close (1,372 at Phase 2 close; +54 in P3.2/P3.3; +27 P3.4 registration tests). Test isolation re-verified at P3.4: all 15 runtime DB checksums identical before/after.
- Runtime/user data (SQLite databases under `var/`) is never committed to git.
- The test suite can no longer silently write into real canonical runtime data (Phase 2 Task 1) — every one of the 10 dashboard-owned path fields now has a session-wide isolated default in `conftest.py`, closing the gap that previously let `role_os_assets.db` grow on every test run. Verified by checksum before/during/after a full 1,315-test `dashboard/tests` run: byte-for-byte unchanged.
- Primary documentation is synced with Role OS 2.0 reality (Phase 2 — Documentation Reality Sync; see `docs/PHASE_2_DOCUMENTATION_REALITY_SYNC.md`): `README.md` and `ARCHITECTURE.md` now describe both layers (v1.x Knowledge/Project Intelligence, and the current Role OS 2.0 Discovery/Workspace/Mission Control chain) instead of only the older one; `dashboard/README.md`'s Configuration table and Project Layout tree now list every 2.0 domain, router, and environment variable (including `ROLE_OS_DISCOVERY_ROOTS`); a "Current State & Continuity" section was added to `README.md` pointing a fresh developer or AI session at this file and `NEXT_ACTIONS.md` first. Two historical docs (`docs/RUNTIME_DATA_MAP.md`, `docs/PHASE_2_MULTI_ROOT_DISCOVERY.md`) got short status notes correcting since-resolved claims, without rewriting their content. `dashboard/app/config.py: app_version` bumped `1.1.0` → `1.2.0` — the task's one pre-approved code change.
- Discovery can scan multiple explicitly-configured roots, not just one (Phase 2 Task 2.1 — see `docs/PHASE_2_MULTI_ROOT_DISCOVERY.md`). `ROLE_OS_DISCOVERY_ROOTS` (comma-separated, optional) is validated and deduplicated (existence, directory-ness, case/slash duplicates, nesting) before scanning; an explicit single-root call, or no multi-root configuration at all, behaves exactly as before. Live validation (isolated temp workspace DB, real filesystem, read-only) confirmed 6 previously-invisible real projects (`role-content-factory`, `rolevaldez.com`, `AGUA-AZUL-APP`/`agua-azul-app`, `charcos-site`, `desierto-creativo-site`) become discoverable under the repo's default root plus `C:\Users\rolev\Documents` — none were adopted; the real canonical `role_os_workspace.db` was never opened during validation (checksum-verified unchanged, matching Task 1's own recorded values).
- Mission Control's daily-use experience was directly re-verified live against canonical data (Phase 2 — Mission Control Daily-Use Gap Check; see `docs/PHASE_2_MISSION_CONTROL_DAILY_USE_REVIEW.md`): all four daily questions (Where did I leave off / What matters now / What should I do next / Can I continue working) scored CLEAR, zero clicks required to see any answer, one click to act (Resume Work). Confirmed Multi-Root Discovery has no effect on Mission Control's output (its whole pipeline reasons only over adopted projects; `total_projects_tracked` stayed at the canonical 5 with `ROLE_OS_DISCOVERY_ROOTS` unset) — specifically re-verified for `bolsa-de-trabajo` too (absent from the scan cache, portfolio, and ranked projects, exactly as expected since it is outside every configured root and unadopted). Outcome: no change needed — two MINOR, non-blocking findings documented (Primary Focus card's height delays scrolling to the next two cards on a typical viewport; one Executive Decision sub-field can read awkwardly when built from a raw historical commit message), neither implemented (did not clear the task's strict change filter as a safely-trivial fix). A forward-looking note (not implemented) was recorded: an isolated repository like `bolsa-de-trabajo` may eventually justify explicit project registration rather than broad home-directory scanning.
- `pi_ai_workspace` (v1.3) vs. `pi_ai_sessions`/`ai_session_snapshots` (v1.4) resolved with evidence (Phase 2 — Runtime Data Hygiene Bundle, part c; see `docs/PHASE_2_PI_AI_WORKSPACE_REVIEW.md`): **LEGACY + CURRENT**, not a duplicate/overlap. `ai_workspace` has 0 rows in both candidate canonical databases (the real, `ROLE_OS_WORKSPACE_DIR`-selected `ROLE_KNOWLEDGE_OS\00_SYSTEM\role_os_projects.db`, and the repo-root fallback `var/role_os/role_os_projects.db`), and zero callers anywhere outside its own isolated router/tests/reconciliation logic. `ai_sessions`/`ai_session_snapshots` has real data (6 sessions, 1 snapshot in the canonical DB) and is the sole authoritative source for Resume Work and Mission Control's Primary Focus. Recommendation: deprecate `ai_workspace` in a future, separately-approved task (not this one — no code changed, no data touched, no removal performed). Read-only inspection also surfaced a new, unrelated finding: `var/role_os/role_os_projects.db` now exists (it did not when `docs/RUNTIME_DATA_MAP.md` was written) and diverges from the real canonical copy — disclosed, not resolved, out of this task's scope.
- Runtime Data Hygiene Bundle's final two parts are complete (Phase 2; see `docs/PHASE_2_RUNTIME_DATA_HYGIENE.md`): the legacy `dashboard/var/role_os_dashboard/` copy was deleted after a full read-only comparison proved every remaining difference from canonical was debug/test data (9 `adopted_projects` rows pointing at confirmed-nonexistent temp folders; all 92 `asset_overrides` rows traced to specific pytest test functions) — the 5 real adopted projects and full 7-row registry were already safely in canonical (Task 3D, 2026-09-10). ROLE MASTER's status mismatch was resolved as historical-legacy-only: current evidence (the project's own `MASTER_INDEX.md`/`TODO.md`, `PROJECT_REGISTRY.md`, and real post-milestone file activity) confirms canonical `adopted_projects.status = "active"` is correct — "Completado" described a completed v1.0 version milestone, not project closure. No runtime data was changed for Part B. `var/role_os_alpha/` remained untouched throughout (checksums/mtimes re-verified identical).

## Current Runtime Model

Two canonical runtime roots, both resolved from the repository root regardless of launch directory (`dashboard/app/config.py: Settings.repo_root`, derived from `__file__`, never CWD):

- **`var/role_os/`** — Knowledge/Project Intelligence/Advisor/Imports/Extraction family. Currently empty by default on this repository; the real, live data for this family lives **externally**, at `ROLE_KNOWLEDGE_OS\00_SYSTEM\`, selected via the persistent `ROLE_OS_WORKSPACE_DIR` environment variable — this is correct, intentional, and must not be copied into the repo.
- **`var/role_os_dashboard/`** — Workspace/Session/Assets/Ecosystem family. Holds the real, migrated canonical state: 5 adopted projects, 7 Daily Session registry entries.

Full inventory, provenance evidence, and the reasoning behind every decision above: **`docs/RUNTIME_DATA_MAP.md`**. Do not re-derive this from scratch — read that file first.

## Canonical Projects

The 5 real adopted projects, currently and only these:

- ROLE OS
- ROLE_KNOWLEDGE_OS
- ROLE Commerce Factory
- ROLE MASTER
- role-ecosystem

This list comes from `var/role_os_dashboard/role_os_workspace.db: adopted_projects`, not from `samples/`, not from `var/role_os_alpha/`. If a future session sees a different count, something changed — check `docs/RUNTIME_DATA_MAP.md` and `git log` before trusting either source blindly.

## Open Issues / Decisions

Per Role's Phase 2 approval: `var/role_os_alpha/` and the 6 out-of-scope real projects remain explicitly **DEFERRED** — do not modify, migrate, delete, rename, or reinterpret them without Role's separate, explicit authorization, regardless of what any other Phase 2 task might suggest is convenient. The legacy `dashboard/var/role_os_dashboard/` copy and the ROLE MASTER status mismatch (formerly listed here) are **RESOLVED** — see below.

1. **`var/role_os_alpha/` disposition** — DEFERRED by Role decision. Contains real-sounding, non-overlapping project data (Kontoor, Unger, Charcos, SUPER FACIL, RoleValdez) of uncertain provenance. Not merged, not discarded (see `docs/RUNTIME_DATA_MAP.md`, "role_os_alpha Assessment").
2. **Real projects outside the Discovery root** — `role-content-factory`, `rolevaldez.com`/`subir-libros-etsy` (Task 6), plus `SUPER-FACIL`, `AGUA-AZUL-APP`/`agua-azul-app`, `charcos-site`, `desierto-creativo-site` (Task 9). **Multi-Root Discovery (Task 2.1) is now built and validated** — configuring `ROLE_OS_DISCOVERY_ROOTS` to include `C:\Users\rolev\Documents` makes 5 of these 6 (all but `SUPER-FACIL`, which no longer exists at any path under `Documents`) discoverable. **None are adopted, and `ROLE_OS_DISCOVERY_ROOTS` is not yet set in any real running configuration** — Task 2.1 built and proved the capability only; whether to actually enable it, and whether to subsequently adopt anything, remains Role's decision (see `docs/PHASE_2_MULTI_ROOT_DISCOVERY.md`, "Known Limitations"). Do not auto-adopt.
3. **`bolsa-de-trabajo`** — real, **ACTIVE** project at `C:\Users\rolev\bolsa-de-trabajo` (`origin` `git@github.com:rolevc-valdez/bolsa-de-trabajo.git`). Do NOT describe it as missing. Outside every Discovery root. **P3.4: explicitly REGISTERED** (`registered_projects` id `d560e18f1f2b3339`) — Role OS now knows it; it is **NOT adopted and NOT classified**, and has zero effect on Mission Control. Whether to adopt it (and with which domain) is Role's decision. Do not add `C:\Users\rolev` as a Discovery root — registration refuses the user profile by design.
4. **`pi_ai_workspace` (legacy v1.3 fields) vs. `pi_ai_sessions` (v1.4)** — **RESOLVED (review complete)**, see `docs/PHASE_2_PI_AI_WORKSPACE_REVIEW.md`: Legacy + Current (Classification B), not a duplicate. `ai_workspace` has 0 live rows anywhere and zero callers outside its own router; `ai_sessions`/`ai_session_snapshots` is the active, sole authoritative source for Resume Work/Mission Control. Recommendation: deprecate `ai_workspace` — but that deprecation itself (marking it deprecated in docs/comments, let alone removing it) is **NOT done here** and requires a separate, explicitly-approved task. Do not remove, rename, or modify `ai_workspace` without that separate approval.
5. **Advisor vs. Operational Intelligence / Executive Decision overlap** — DEFERRED. Do not redesign or remove Advisor in Phase 2.
6. **Stale top-level `README.md`/`ARCHITECTURE.md`/`app_version`** — **RESOLVED** by Phase 2's Documentation Reality Sync task; see `docs/PHASE_2_DOCUMENTATION_REALITY_SYNC.md`. Remaining, deliberately-out-of-scope documentation debt: `CHANGELOG.md`/`docs/product/CHANGELOG_PRODUCT.md` have no Phase 2 entries yet (not misleading, just not yet appended to).
7. **`dashboard/var/role_os_dashboard/` (the pre-migration legacy copy)** — **RESOLVED, DELETED** (Phase 2 — Runtime Data Hygiene; see `docs/PHASE_2_RUNTIME_DATA_HYGIENE.md`). A full read-only comparison proved every remaining difference from canonical was debug/test data; all real user data was already safely in canonical since Task 3D (2026-09-10).
8. **ROLE MASTER status mismatch** — **RESOLVED, no change needed** (Phase 2 — Runtime Data Hygiene; see `docs/PHASE_2_RUNTIME_DATA_HYGIENE.md`). Current evidence (the project's own `MASTER_INDEX.md`/`TODO.md`, `PROJECT_REGISTRY.md`, real post-milestone file activity) confirms canonical `adopted_projects.status = "active"` is correct — the legacy "Completado" described a completed v1.0 version milestone, not project closure. No runtime data was changed.

## Recovery

1. Read this file (`CURRENT_STATE.md`).
2. Read `NEXT_ACTIONS.md`.
3. Run `git status` and `git log -5` — confirm the repository actually matches what this file claims (if it doesn't, trust the repository, not this file, and treat the mismatch itself as the first thing to investigate).
4. Continue only the task named in `NEXT_ACTIONS.md`'s "Now" section.

**Do NOT repeat any task listed above as completed.** Full implementation detail for Phase 1 is in `docs/PHASE_1_TASK_*.md` and `docs/PHASE_1_COMPLETION.md`; for Phase 2, in `docs/PHASE_2_TASK_*.md` and `docs/ROLE_OS_2_PHASE_2_PLAN.md` — read the specific doc if you need to understand *how* something was done, not this file.
